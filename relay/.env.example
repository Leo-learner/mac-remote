# Relay configuration. Secrets are written by: node relay/setup.js --env relay/.env
PORT=3030
HOST=127.0.0.1
PUBLIC_ORIGIN=https://control.dkz12345.com
RELAY_DATA_DIR=/opt/apps/mac-remote-relay/relay/data
PASSWORD_HASH=
TOTP_SECRET=
# Printed by `node agent/setup.js <relay-url>` on the Mac
AGENT_TOKEN_SHA256=
